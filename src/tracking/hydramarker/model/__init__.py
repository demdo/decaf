"""HydraMarker model-building package.

The package contains observation recording, SfM reconstruction, bundle
adjustment, diagnostics, visualization, and export helpers used to create the
metric marker geometry consumed by the tracker runtime.
"""
